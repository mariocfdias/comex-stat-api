# Dependências Python para as DAGs do Airflow
# Instalar no container Airflow via: pip install -r requirements.txt

apache-airflow[postgres,amazon]==2.9.*
apache-airflow-providers-postgres>=5.0.0
apache-airflow-providers-amazon>=8.0.0  # para MinIO/S3

# Processamento de dados
pandas>=2.0.0
pyarrow>=14.0.0       # Parquet I/O
python-dateutil>=2.8.0
requests>=2.31.0
urllib3>=2.0.0

# Geoespacial (para dag_sigmine_ingestao)
geopandas>=0.14.0
fiona>=1.9.0
shapely>=2.0.0

# PostgreSQL
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# MinIO / S3
boto3>=1.34.0
minio>=7.2.0

# dbt (para executar transformações nas DAGs)
dbt-postgres>=1.7.0

# Utilitários
python-dotenv>=1.0.0
